//================ Datatypes in RUST ==================

fn main(){
    let age:u8=30;  //unsigned int

    let percentile:f32=99.678484;  //float 32

    let is_logout:bool=true;

    let letter:char='R'; //use single quotes

    println!("age is:{}, Percentile scored = {} and user is_logout={},Rust starts with:{}",age,percentile,is_logout,letter);
}